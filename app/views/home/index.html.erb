
<div id="banner">
  <h1>myTour</h1>
  <h2>We put musicians in control.</h2>
  <h2>Source jam space, crash space, backline technicians, gear rentals, vehicle rentals, merchandise sellers, and more. Book resources in any city to support your own tour.</h2>
</div>
<div class="services">
  <div id="services_head">
    <h1>Open Resources</h1>
  </div>
  <div class='row service_row'>
    <% @services.each do |service| %>
    <div class='service_div'>
      <img class='service_img'><%= image_tag service.image.url(:medium) %></img>
      <h3><%= service.name %></h3>
      <div><%= sanitize(service.description) %></div>
      <div><%= number_to_currency(service.price) %></div>
      <%= button_to 'Book this Service', new_booking_path(service_id: service), class: 'btn btn-dark btn-sm', remote: true %>
    </div>
    <% end %>
  </div>
</div>
